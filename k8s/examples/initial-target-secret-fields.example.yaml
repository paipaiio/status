# Optional fields for api-relay-status-secret.
# You only need these when you want K8s Secret to seed fixed targets at startup.
# If you add targets from /admin, they are persisted in PVC:
# /data/config/targets.json
stringData:
  API_1_NAME: "主用中转站"
  API_1_BASE_URL: "https://api.example.com/v1"
  API_1_MODEL: "gpt-4o-mini"
  API_1_API_KEY: "sk-your-real-key"
  API_1_FORMAT: "auto"
  API_1_CHECK_MODE: "auto"
