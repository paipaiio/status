{
  "name": "api-relay-status-site",
  "version": "0.1.0",
  "private": true,
  "description": "API relay status dashboard with backend scheduled checks and sanitized frontend data.",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "qq:bot": "node src/qq-status-bot.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
